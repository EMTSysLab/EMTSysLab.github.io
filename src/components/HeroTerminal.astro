---
// HeroTerminal.astro — Hero centrado con avatar y terminal a ancho completo
---

<div class="hero-custom">

  <div class="hero-top">
    <img src="/avatar.png" alt="EMTSysLab" class="hero-avatar" />
    <h1 class="hero-title">
      <span class="hero-title-main">EMTSysLab</span>
      <span class="hero-title-sub">Learn & Automate IT</span>
    </h1>
    <p class="hero-description">
      Un laboratorio personal hecho público. Casos reales, soluciones probadas en producción
      y documentación que uso yo mismo — por si a ti también te salva el día.
      <br/><br/>
      <span style="font-size:0.9rem;color:#6c7086;font-style:italic;">Porque las mejores soluciones no deberían quedarse en un solo escritorio.</span>
    </p>
    <div class="hero-actions">
      <a href="/linux/auditoria-linux-ens-caso-real/" class="btn-primary">Explorar documentación →</a>
      <a href="https://github.com/EMTSysLab" class="btn-secondary" target="_blank">GitHub</a>
    </div>
  </div>

  <div class="hero-terminal">
    <div class="terminal-header">
      <span class="terminal-dot red"></span>
      <span class="terminal-dot yellow"></span>
      <span class="terminal-dot green"></span>
      <span class="terminal-title">emt@syslab:~</span>
    </div>
    <div class="terminal-body" id="terminal-body">
      <div class="terminal-output" id="terminal-output"></div>
      <div class="terminal-cursor-line">
        <span class="terminal-prompt">$ </span>
        <span id="terminal-typing"></span>
        <span class="terminal-cursor">█</span>
      </div>
    </div>
  </div>

  <div class="hero-stats">
    <div class="stat">
      <span class="stat-number">5</span>
      <span class="stat-label">Artículos</span>
    </div>
    <div class="stat-divider"></div>
    <div class="stat">
      <span class="stat-number">4</span>
      <span class="stat-label">Secciones</span>
    </div>
    <div class="stat-divider"></div>
    <div class="stat">
      <span class="stat-number">100%</span>
      <span class="stat-label">Producción real</span>
    </div>
  </div>

</div>

<style>
.hero-custom {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 1.5rem 2rem 1.5rem;
  max-width: 860px;
  margin: 0 auto;
  text-align: center;
}

.hero-avatar {
  width: 80px;
  height: 80px;
  border-radius: 50%;
  object-fit: cover;
  border: 3px solid rgba(203, 166, 247, 0.5);
  margin-bottom: 0.75rem;
  box-shadow: 0 0 30px rgba(203, 166, 247, 0.2);
}

.hero-badge {
  display: inline-block;
  background: rgba(203, 166, 247, 0.12);
  border: 1px solid rgba(203, 166, 247, 0.3);
  color: #cba6f7;
  padding: 0.25rem 0.85rem;
  border-radius: 999px;
  font-size: 0.75rem;
  margin-bottom: 1rem;
  font-family: monospace;
}

.hero-title {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin: 0 0 1rem;
  line-height: 1.1;
}

.hero-title-main {
  font-size: 3.2rem;
  font-weight: 800;
  background: linear-gradient(135deg, #cba6f7, #89b4fa, #94e2d5);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.hero-title-sub {
  font-size: 1.1rem;
  color: #a6adc8;
  font-family: monospace;
  font-weight: 400;
  margin-top: 0.3rem;
}

.hero-description {
  color: #cdd6f4;
  font-size: 1.05rem;
  line-height: 1.7;
  margin-bottom: 1.25rem;
  max-width: 580px;
}

.hero-actions {
  display: flex;
  gap: 1rem;
  margin-bottom: 1.5rem;
  justify-content: center;
  flex-wrap: wrap;
}

.btn-primary {
  background: #cba6f7;
  color: #1e1e2e;
  padding: 0.75rem 1.5rem;
  border-radius: 8px;
  text-decoration: none;
  font-weight: 700;
  font-size: 0.95rem;
  transition: all 0.2s;
}

.btn-primary:hover {
  background: #b4a0f0;
  transform: translateY(-2px);
}

.btn-secondary {
  background: transparent;
  color: #cdd6f4;
  padding: 0.75rem 1.5rem;
  border-radius: 8px;
  text-decoration: none;
  font-weight: 600;
  font-size: 0.95rem;
  border: 1px solid rgba(205, 214, 244, 0.3);
  transition: all 0.2s;
}

.btn-secondary:hover {
  border-color: #cba6f7;
  color: #cba6f7;
}

.hero-terminal {
  width: 100%;
  background: #11111b;
  border-radius: 12px;
  border: 1px solid rgba(203, 166, 247, 0.2);
  overflow: hidden;
  box-shadow: 0 25px 60px rgba(0,0,0,0.5);
  margin-bottom: 1.5rem;
}

.terminal-header {
  background: #181825;
  padding: 0.75rem 1rem;
  display: flex;
  align-items: center;
  gap: 0.5rem;
  border-bottom: 1px solid rgba(255,255,255,0.05);
}

.terminal-dot {
  width: 12px;
  height: 12px;
  border-radius: 50%;
}

.terminal-dot.red    { background: #f38ba8; }
.terminal-dot.yellow { background: #f9e2af; }
.terminal-dot.green  { background: #a6e3a1; }

.terminal-title {
  font-family: monospace;
  font-size: 0.8rem;
  color: #6c7086;
  margin-left: 0.5rem;
}

.terminal-body {
  padding: 1.25rem;
  font-family: monospace;
  font-size: 0.875rem;
  line-height: 1.8;
  height: 290px;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
  padding-top: 1rem;
  text-align: left;
  color: #cdd6f4;
}

.terminal-output {
  overflow: hidden;
}

.terminal-cursor-line {
  display: flex;
  align-items: center;
}

.terminal-prompt {
  color: #a6e3a1;
  margin-right: 0.25rem;
}

.terminal-cursor {
  animation: blink 1s step-end infinite;
  color: #cba6f7;
  margin-left: 1px;
}

@keyframes blink {
  0%, 100% { opacity: 1; }
  50%       { opacity: 0; }
}

.hero-stats {
  display: flex;
  align-items: center;
  gap: 2rem;
}

.stat {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.stat-number {
  font-size: 1.6rem;
  font-weight: 800;
  color: #cba6f7;
  font-family: monospace;
}

.stat-label {
  font-size: 0.7rem;
  color: #a6adc8;
  text-transform: uppercase;
  letter-spacing: 0.08em;
}

.stat-divider {
  width: 1px;
  height: 36px;
  background: rgba(255,255,255,0.1);
}

.output    { color: #a6adc8; }
.success   { color: #a6e3a1; }
.warning   { color: #f9e2af; }
.error     { color: #f38ba8; }
.prompt-line { color: #a6e3a1; }
</style>

<script>
const MAX_LINES = 10;

const sequences = [
  {
    cmd: "sestatus",
    output: [
      'SELinux status:        <span style="color:#a6e3a1">enabled</span>',
      'SELinux mount policy:  <span style="color:#a6e3a1">enforcing</span>',
      'Policy MLS status:     <span style="color:#a6e3a1">enabled</span>',
    ]
  },
  {
    cmd: "kubectl get nodes",
    output: [
      '<span style="color:#a6adc8">NAME        STATUS   ROLES          AGE</span>',
      '<span style="color:#a6e3a1">master-01   Ready    control-plane  42d</span>',
      '<span style="color:#a6e3a1">master-02   Ready    control-plane  42d</span>',
      '<span style="color:#a6e3a1">master-03   Ready    control-plane  42d</span>',
      '<span style="color:#a6e3a1">worker-01   Ready    worker         42d</span>',
      '<span style="color:#a6e3a1">worker-02   Ready    worker         42d</span>',
      '<span style="color:#a6e3a1">worker-03   Ready    worker         42d</span>',
    ]
  },
  {
    cmd: "ollama run llama3.1:8b",
    output: [
      '<span style="color:#a6adc8">Cargando modelo llama3.1:8b...</span>',
      '<span style="color:#a6e3a1">Sin anomalías críticas detectadas.</span>',
      '<span style="color:#a6adc8">CPU load normal. Memoria al 67%.</span>',
    ]
  },
  {
    cmd: "podman ps",
    output: [
      '<span style="color:#a6adc8">NAMES           STATUS</span>',
      '<span style="color:#a6e3a1">nginx-prod      Up 12 days</span>',
      '<span style="color:#a6e3a1">postgres-db     Up 12 days</span>',
      '<span style="color:#a6e3a1">redis-cache     Up 5 days</span>',
    ]
  },
  {
    cmd: "systemctl is-active firewalld auditd sshd",
    output: [
      '<span style="color:#a6e3a1">active</span>',
      '<span style="color:#a6e3a1">active</span>',
      '<span style="color:#a6e3a1">active</span>',
    ]
  }
];

let currentSeq = 0;
let lines = [];

const typingEl = document.getElementById("terminal-typing");
const outputEl = document.getElementById("terminal-output");

function sleep(ms) {
  return new Promise(r => setTimeout(r, ms));
}

function render() {
  outputEl.innerHTML = lines.slice(-MAX_LINES).map(l => `<div style="min-height:1.4em;white-space:pre;font-family:monospace">${l}</div>`).join("");
}

async function typeCommand(cmd) {
  typingEl.textContent = "";
  for (let ch of cmd) {
    typingEl.textContent += ch;
    await sleep(60 + Math.random() * 35);
  }
}

async function runSequence() {
  await sleep(800);
  while (true) {
    const seq = sequences[currentSeq % sequences.length];

    // Escribir comando letra a letra
    await typeCommand(seq.cmd);
    await sleep(500);

    // Pasar comando al historial
    lines.push('<span style="color:#a6e3a1">$ </span><span style="color:#cdd6f4">' + seq.cmd + '</span>');
    typingEl.textContent = "";
    render();

    // Mostrar output línea a línea
    for (let line of seq.output) {
      lines.push(line);
      render();
      await sleep(220);
    }

    // Pausa para leer el resultado
    await sleep(2500);

    // Limpiar silenciosamente
    await sleep(300);
    lines = [];
    render();
    await sleep(400);

    currentSeq++;
  }
}

runSequence();
</script>
